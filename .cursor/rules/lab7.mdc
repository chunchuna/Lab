---
description: LAB 7 等距 2D 游戏项目的开发约定与架构契约
alwaysApply: true
---

# LAB 7 开发约定

## 分工：只做开发，不做玩法测试

- **玩法测试由用户负责。** 不要写测试套件、不要跑成批截图脚本、不要做视觉验收，
  除非用户明确要求。
- 你的交付标准只有两条：
  1. 每个改过的 JS 文件通过 `node --check`；
  2. 游戏能正常启动、改动的交互能跑通、控制台无相关报错。
- 达到上面两条就算完成，直接汇报，不要自行追加"手感是否好""画面是否好看"这类判断。
- 需要人工看一眼的东西，直接说明"请你试一下 X"，而不是自己反复截图猜。
- 想快速进入游戏可以在控制台调 `window.__skipIntro()`，跳过标题和起床过场。
  调试钩子：`__game`、`__inv`、`__pad`、`__level`、`__render`。

## 目标平台：APK + TapPlay，不走小游戏

- 分发形态是 **Android APK（TapPlay 沙箱启动）**，另外兼顾 itch.io / 自部署网页 /
  桌面浏览器，未来可能用 Tauri 或 Electron 打 exe 上 Steam。
- **不要为了兼容微信/抖音/TapTap 小游戏而把 UI 改成 canvas 绘制。** 小游戏运行环境
  没有 BOM/DOM，那条路等于重写整个 UI 层，已经明确放弃。
- 合规约束会反过来限制代码，动手前想清楚：
  - **不能加内购或付费解锁**，否则需要游戏版号。
  - **不能接微信/QQ 等第三方登录**，TapPlay 明确禁止。
  - **不要随便加联网请求**。一旦联网，按 TapTap 资质要求就得办 APP 的 ICP 备案。
    目前游戏完全离线。

## 渲染架构：canvas 画世界，DOM 画 UI

这是刻意的分工，不要打破：

- **canvas（内部 640×360，整体放大）**：地板、墙、道具、角色、光照、粒子。
- **DOM + CSS**：所有 UI —— 目标提示、字幕、双手面板、弹药、背包（含立绘与拖拽）、
  人脸识别终端、标题、竖屏引导、触屏摇杆与按钮。
- 理由：内部分辨率只有 640×360，中文画在同一张 canvas 上会小到不可读；DOM 还白送
  布局引擎、CSS 动画和命中测试。已实测 DOM UI 的每帧开销在噪声范围内，不是瓶颈。
- 新增 UI 一律用 DOM，写进 `index.html` + `style.css`，不要画到 canvas 上。
- 美术全部程序化生成（`art.js`），**不引入任何图片资源**，也保持零运行时依赖。

## 输入：必须走统一意图层

- 玩法代码**不要**直接读 `keys` 或触摸事件。移动读 `moveIntent()`，朝向读
  `player.aim`，动作走 `doAction(act)`。
- **每加一个新交互，键盘绑定和触屏按钮都要一起加**，否则手机上就是残废功能：
  - 键盘：在 `initInput` 的 `onKey` 里加分支；
  - 触屏：在 `index.html` 的 `#pad` 加按钮 + `style.css` 定位 + `controls.js` 的
    动作分发，并在 `doAction` 里复用同一个函数。
- 触屏按钮区是有限资源。底部两侧是拇指区，新按钮只能放在右侧拇指弧上，
  **触控目标不小于 44px，且不能互相重叠**。上下文相关的动作（如"互动"）应该
  按需显示，不要常驻占位。

## 已经踩过的坑，别再踩

- CSS 的 `bottom` / `width` / `height` 用 `em` 时，是按**元素自身**的 `font-size`
  计算的。在同一个元素上既写 `font-size: 1.4em` 又写 `width: 7em`，尺寸会被放大
  一轮。字号只设在内层 `span` 上。
- `<button>` **默认不继承 `font-size`**（走 UA 的 13.3px，是绝对值，屏幕变大不会
  跟着缩放）。`.pbtn` 必须显式写 `font-size: inherit`。
- `#stage` 有 `transform`，会成为 `position: fixed` 的包含块。想覆盖整个视口的
  元素（如竖屏引导）必须放在 `#stage` **外面**，否则会被它的 `overflow: hidden` 裁掉。
- ES module 在 `file://` 下会被 CORS 拦住。APK 里跑的是 `npm run build` 产出的
  IIFE 单文件，**不要**在 `index.html` 里新增 `type="module"` 之外的加载假设。
- 系统字体缩放会撑坏 em 布局，APK 侧已用 `setTextZoom(100)` 锁死。

## 光照：静态光要烘焙

- 静态光源位置与遮挡整局不变，只有亮度在闪，所以启动时按满功率烘焙成贴图
  （`lighting.bakeLight`），每帧只做一次 `drawImage` + `globalAlpha`。
  **新增静态光源要照此烘焙**，不要走每帧重算的 `add()`。
- 镜头抖动是整体平移，画烘焙贴图时传入同样的 `shx/shy` 偏移即可对齐。
- 每帧的 canvas `filter: blur()` 很贵，只有手电筒的动态光锥在用。不要随手再加。
- 参考成本：单帧渲染约 1.1ms（60fps 预算 16.7ms），撑到 6 倍 CPU 降速仍满帧。
  加了重开销的东西请留意别把这个余量吃光。

## 视线遮挡：代码保留，默认关闭

`game.losOcclusion` 默认 `false`，`V` 键切换。关闭时用预烘焙的房间轮廓
（`level.roomVis` / `ROOM_MASK`）裁剪，**光线遮挡（阴影）始终生效**。
不要因为默认关闭就删掉 `computeVisibility` 这条路径。
