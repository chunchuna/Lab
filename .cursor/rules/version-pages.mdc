---
description: 版本号展示，以及改完后直接提交并部署 GitHub Pages
alwaysApply: true
---

# 版本号与 GitHub Pages

## 屏幕下方必须显示当前版本

- 版本写在 `src/version.js` 的 `VERSION`（例如 `'1.1.0'`）。
- `#build` 必须放在 `#stage` **外面**，`position: fixed` 贴浏览器视口底边。
  舞台有 `transform`，放里面会被裁掉，窗口上下黑边时也看不见。
- 由 `ui.js` 在 `initUI` 写入 `v` + `VERSION`；`index.html` 里先写一份同号作兜底。
  **不要画到 canvas 上。**
- `package.json` 的 `version` 必须和 `VERSION` 对齐。
- `index.html` 里 `style.css` 和 `src/main.js` 的 `?v=` 查询串也要改成同一个号，
  避免 GitHub Pages / 浏览器缓存还显示上一版。

## 每次更新先升版本号

改任何会进游戏或进站点的内容之前，先把 `VERSION` 升一档：

- 小修、文案、样式、规则：patch（`1.1.1` → `1.1.2`）
- 新功能、新场景、流程改动：minor（`1.1.1` → `1.2.0`）
- 大改、不兼容重做：major（`1.1.1` → `2.0.0`）

漏升版本号等于没做完。

## 改完后直接提交并推送，不等 PR 审批

用 `git branch --show-current` 看当前工作分支（目前是 `cursor/dawn-lighting-rays-ed8a`）。
用户要求：**改完就推这条分支，不要走 PR 审批。**

1. 升版本号
2. 改过的 JS 跑 `node --check`
3. `git add` / `git commit` / `git push origin <当前分支>`
4. `.github/workflows/pages.yml` 只监听 `cursor/rooftop-prologue-7a6f`，推别的分支
   不会触发 https://chunchuna.github.io/Lab/ 的部署。要让当前分支上线，先问用户
   是否改 workflow 的分支过滤，不要自己顺手改。

不要只改完不推。不要新开 PR 等用户点批准。要合进 `pc` 时等用户说。

## 本地直接开发，不派云端子代理

代码在本机，Agent 直接用 Cursor 的本地工具读写、跑 `node`、起 `npm start` 验证。
不要把任务转交云端代理，也不要为了"看一眼"去克隆仓库。全局的 `code-changes` 技能
里那套 CloudAgent 流程对本项目**不适用**，忽略它。
